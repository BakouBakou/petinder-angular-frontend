import { PopularityLabelPipe } from './popularity-label.pipe';

describe('PopularityLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new PopularityLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
